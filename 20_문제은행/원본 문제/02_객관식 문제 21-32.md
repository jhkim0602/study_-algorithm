# 02_객관식 문제 21-32

- 원본 사진: [[20_문제은행/원본 문제/사진/IMG_8869.JPG]]
- 재작성 기준: 사진 원문을 기준으로 객관식 문장과 선택지를 순서대로 정리했다.

## 원문 표기

`ch02. 기본 자료구조와 배열 / ch03. 검색 알고리즘`

`[객관식 선택형 문제]`

### 문제 21
자료구조(data structure)에 대한 설명으로 가장 적절한 것은?

① 데이터를 화면에 출력하는 방법
② 데이터를 저장하는 메모리의 크기
③ 데이터를 효율적으로 관리하기 위한 데이터의 조직 방식
④ 데이터를 입력받는 방법

### 문제 22
다음 기수 변환 코드에서 마지막에 `[::-1]`을 사용하는 이유는?

① 정렬하기 위해
② 중복을 제거하기 위해
③ 나머지를 구한 순서가 거꾸로이기 때문에
④ 문자열을 리스트로 바꾸기 위해

### 문제 23
배열의 모든 원소를 앞에서부터 차례대로 검사하는 알고리즘은?

① 이진 검색
② 검색
③ 선형 검색
④ 깊이 우선 검색

### 문제 24
선형 검색의 시간 복잡도는?

① `O(1)`
② `O(log n)`
③ `O(n)`
④ `O(n^2)`

### 문제 25
다음 보초법의 장점으로 가장 적절한 것은?

① 배열이 자동 정렬된다
② 종료 조건 검사의 수를 줄일 수 있다
③ 항상 `O(log n)`이 된다
④ 추가 메모리가 전혀 필요 없다

### 문제 26
이진 검색의 특징으로 옳은 것은?

① 데이터가 정렬되지 않아도 사용할 수 있다
② 항상 배열의 첫 번째 원소부터 검사한다
③ 데이터가 정렬되어 있어야 사용할 수 있다
④ 모든 원소를 검사해야 한다

### 문제 27
다음 이진검색에서 검색 실패의 종료 조건은?

① `pl == pr`
② `pc == 0`
③ `pl > pr`
④ `a[pc] != key`

### 문제 28
원소 수가 16개인 정렬 배열에서 이진검색의 최대 비교 횟수로 가장 적절한 것은?

① 4
② 8
③ 15
④ 16

### 문제 29
이진 검색에서 중앙 인덱스 `pc` 계산식은?

① `pc = pl + pr`
② `pc = (pl + pr) // 2`
③ `pc = pl * pr`
④ `pc = pr - pl`

### 문제 30
해시법의 특징으로 옳은 것은?

① 데이터를 항상 정렬해야 한다
② 키를 이용하여 배열의 저장 위치를 계산한다
③ 모든 데이터를 순서대로 검사한다
④ 배열의 마지막부터 탐색한다

### 문제 31
다음 중 해시 충돌(collision)에 대한 설명으로 옳은 것은?

① 같은 키가 두 번 저장되는 현상
② 서로 다른 키가 같은 해시값을 갖는 현상
③ 정렬이 되지 않은 현상
④ 연결 리스트가 끊어지는 현상

### 문제 32
체인법 해시에서 검색의 최악 시간복잡도는?

① `O(1)`
② `O(log n)`
③ `O(n)`
④ `O(n^2)`
